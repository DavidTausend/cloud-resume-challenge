AWSTemplateFormatVersion: '2010-09-09'
Transform: AWS::Serverless-2016-10-31
Description: Simple counter API

Parameters:
  TableName:
    Type: String
    Default: CounterTable
    Description: DynamoDB table name for the counter
  CounterPrimaryKeyValue:
    Type: String
    Default: global
    Description: The partition key value used for the single counter item

Globals:
  Function:
    Runtime: python3.12
    Timeout: 10
    MemorySize: 256
    Architectures: [x86_64]
    Tracing: Active
    Environment:
      Variables:
        TABLE_NAME: !Ref CounterTable
        COUNTER_PK: !Ref CounterPrimaryKeyValue

Resources:
  Api:
    Type: AWS::Serverless::HttpApi
    Properties:
      CorsConfiguration:
        AllowOrigins:
          - https://davidtausendresume.com
          - https://www.davidtausendresume.com
        AllowHeaders:
          - '*'
        AllowMethods:
          - GET
          - POST
          - OPTIONS
      DisableExecuteApiEndpoint: true
      DefaultRouteSettings:
        ThrottlingBurstLimit: 5
        ThrottlingRateLimit: 10
      Domain:
        DomainName: api-counter.davidtausendresume.com
        CertificateArn: arn:aws:acm:eu-central-1:517392055929:certificate/90adbb74-6360-4903-9e8a-a53bcedf976e
        EndpointConfiguration: REGIONAL
        Route53:
          HostedZoneId: Z06283332HJKQ2EI8ZM4S

  CounterTable:
    Type: AWS::DynamoDB::Table
    Properties:
      TableName: !Ref TableName
      BillingMode: PAY_PER_REQUEST
      AttributeDefinitions:
        - AttributeName: pk
          AttributeType: S
      KeySchema:
        - AttributeName: pk
          KeyType: HASH
      SSESpecification:
        SSEEnabled: true

  CounterFunction:
    Type: AWS::Serverless::Function
    Properties:
      CodeUri: src/
      Handler: function.lambda_handler
      Policies:
        - DynamoDBCrudPolicy:
            TableName: !Ref CounterTable
      Events:
        GetCounter:
          Type: HttpApi
          Properties:
            ApiId: !Ref Api
            Path: /counter
            Method: GET
        IncrementCounter:
          Type: HttpApi
          Properties:
            ApiId: !Ref Api
            Path: /counter
            Method: POST

Outputs:
  ApiEndpoint:
    Description: Base URL for the HTTP API
    Value: !Sub 'https://${Api}.execute-api.${AWS::Region}.amazonaws.com'
  TableOut:
    Description: DynamoDB table name
    Value: !Ref CounterTable
  FunctionArn:
    Description: Lambda function ARN
    Value: !GetAtt CounterFunction.Arn